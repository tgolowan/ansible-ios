---
- hosts: all
  gather_facts: no
  connection: local
  
  tasks:
  - name: show running config
    ios_command:
      commands: show running-config
    register: Print_output
  
  - debug: msg="{{ Print_output.stdout}}"
